{% extends 'base.html.twig' %}

{% block body %}
<h1>Deuchnord Series Manager</h1>

{% for s in series %}
<div id="s{{ s.id }}">
	<h2>{{ s.name }}</h2>
	<p>{{ s.author }}</p>
	<p>{{ s.description }}</p>
</div>
{% endfor %}
{% endblock %}